import {StyleSheet} from 'react-native';

const styles = {
  wrapper: {
    height: 200,
    flexFlow: 'row noWrap',
    justifyContent: 'center',
    alignItems: 'center',
  },
};

export default StyleSheet.create(styles);
